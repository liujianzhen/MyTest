使用多线程的原因：
	1、提高CPU的利用率；（进行IO操作时，CPU是空闲的）
	2、有非常多不同的连接时，给每个连接一个线程

在主线程中启动一个线程之后，主线程会直接继续往下执行，而不需要等待启动的线程执行完成。

使用多线程需要考虑的问题：
	1、对于共享变量进行修改操作需要考虑到线程同步；
	2、线程死锁问题；
	3、线程间的通信（信号量）

CPU的工作过程：先将代码从磁盘加载到内存中，CPU从内存逐条读取指令并执行，寄存器会保存需要执行的下一条指令

如果直接使用形如thread1.run();语句，j
场景：
 单实例多线程情况下 
  启动两个线程，一个线程进行加一操作，加十次变为 11，一个线程进行减一操作，减十次变为 -9 
  
    如果一个变量只是定义在方法中的局部变量，永远不会出现线程安全的问题。
　但是，如果这时候如果想要定义一个变量在这个类中的所有方法中都可以访问的话，就需要定义类的成员变量，此时该变量不是线程安全的。
　为了既是线程安全的，又可以在类的所有方法中访问，此时可以用到 ThreadLocal。